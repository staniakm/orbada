subprojects {
    project.build.doLast {
        println(": adding '${project}' to plugins")
        ant.copy(
            file: project.tasks.jar.archiveFile.get().asFile,
            todir: new File(rootProject.sourceSets.main.output.resourcesDir, "plugins")
        )
    }
}